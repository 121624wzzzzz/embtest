# 论文 A：GCorr（当前口径）

Base→Instruct 相关结论统一使用 **BI-clean 30**。Task1 原始结果仍完整保留35对，但本目录的主表和主结论排除5个异常 pair。

当前任务规模：Task1–4 共342条任务结果，去重后340个唯一模型对，覆盖94个模型。Task1 与 Task2 重叠的两对是 DeepSeek-V3 与 DeepSeek-V3.1 的 Base→Instruct pair。

优先阅读 [`INSIGHTS.md`](INSIGHTS.md)。派生表位于 `tables/`。
